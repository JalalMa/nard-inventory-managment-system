/**
 * Development environment (default, used by `ng serve`).
 * Production values live in `environment.production.ts` and are swapped in
 * via `fileReplacements` during a production build.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
  socketUrl: 'http://localhost:3000',
};
